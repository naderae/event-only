

  <div class="event">
    <h3>Event Name: <%= event.name %></h3> <!-- For partials, you use a local variable instread of the instance variable -->
    <h3>Capacity: <%= event.capacity %></h3>
    <%= event.date %><br />
    <%= link_to(event.name, event_path(event) %>
  </div>

  
